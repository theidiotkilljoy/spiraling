{
  "rules": {
    ".read": false,
    ".write": false,
    "rooms": {
      "$roomId": {
        "settings": {
          ".read": "auth != null",
          "ownerUid": {
            ".write": "auth != null && !data.exists() && newData.isString() && newData.val() == auth.uid",
            ".validate": "newData.isString()"
          },
          "difficulty": {
            ".write": "auth != null && root.child('rooms').child($roomId).child('settings').child('ownerUid').val() == auth.uid",
            ".validate": "newData.isNumber() && newData.val() >= 1 && newData.val() <= 5"
          }
        },
        "rolls": {
          ".read": "auth != null",
          "$rollId": {
            ".write": "auth != null",
            ".validate": "newData.val() == null || (newData.hasChildren(['player','totalDice','panicDice','advantage','pools','selected','result','panicAdded','panicRemoved','difficulty','revealAt','createdAt']) && newData.child('player').isString() && newData.child('player').val().length <= 32 && newData.child('totalDice').isNumber() && newData.child('totalDice').val() >= 1 && newData.child('totalDice').val() <= 5 && newData.child('panicDice').isNumber() && newData.child('panicDice').val() >= 0 && newData.child('panicDice').val() <= 3 && newData.child('difficulty').isNumber() && newData.child('difficulty').val() >= 1 && newData.child('difficulty').val() <= 5)"
          }
        }
      }
    }
  }
}
